# Resolve summary: daemon recovery and test isolation

| issue# | root-cause file:line | fix | test result | RESOLVED/SKIPPED+reason |
|---:|---|---|---|---|
| 539 | `crates/khive-mcp/src/daemon.rs:4052-4135`: the prior concurrency test exercised two direct `kill_and_respawn` calls with a fake listener, so it did not cover eight clients entering through `forward_or_spawn`, prove a usable surviving daemon, or cover concurrent terminal post-write `ParseFailure`. | Added a test-only in-process daemon launcher and two N=8 regressions at `crates/khive-mcp/src/daemon.rs:4173-4528`. Remediation added a forced recovery-race barrier, panic-safe RAII cleanup for daemon state and environment, a non-gating spawn-count diagnostic, and a bounded check that rejects a ninth connection. | Both new #539 tests passed 10/10 in isolation; the complete `daemon::tests::` module passed 80/80; the full `khive-mcp` gate passed 206 unit and 109 integration tests. | RESOLVED |
| 544 | Pre-fix `crates/khive-mcp/src/daemon.rs:1851-1979` and `crates/khive-runtime/src/daemon.rs:1337-1680` privately duplicated generic daemon framing/test helpers, and the first implementation left `base_request_frame` and `round_trip` split between the runtime test module and shared support. | Added the feature-gated shared module declared at `crates/khive-runtime/src/daemon.rs:1348`; consolidated the helpers, including `base_request_frame` and `round_trip`, in `crates/khive-runtime/src/daemon_test_support.rs:15-104`; migrated consumers and removed the local duplicates. | `cargo test -p khive-runtime` passed 788 unit, 2 cold-boot integration, and 69 integration tests; `khive-mcp` passed 206 unit and 109 integration tests. The final definition scan found exactly one `base_request_frame` and one `round_trip`. | RESOLVED |
| 557 | `crates/khive-mcp/src/daemon.rs:1767-1785,1868-1886`: explicit `ProtocolMismatch` is observed only after the real request frame is written, and both handling arms deliberately return a hard error plus bridge self-heal without kill, respawn, retry, or local fallback. | No change. The issue's requested recovery-after-write would resend a request that may already have executed, conflicting with the current #644 at-most-once contract. | The focused terminal-path test passed 1/1 and retained its zero-retry assertion; the full `khive-mcp` gate passed. The tester separately recorded a non-gating, pre-existing detached-task socket-bind race in that test fixture. | SKIPPED - the requested respawn behavior is stale and architecturally incompatible with the #644 at-most-once contract. |
| 705 | Pre-fix `.github/workflows/ci.yml:272` ran workspace coverage with default parallel libtest scheduling; `crates/khive-db/src/pool.rs:348-362` bounds writer checkout, and `crates/khive-mcp/src/pending_events.rs:540-560,1756-1796` records a checkout/claim timeout as a failed drain. | Appended `-- --test-threads=1` only to the coverage `cargo llvm-cov` invocation at `.github/workflows/ci.yml:272`; production timeout and retry behavior were unchanged. | The focused pending-events suite passed 31/31 with one test thread during implementation. Final validation confirmed the CI diff is confined to the coverage invocation, `git diff --check` is clean, and the full `khive-mcp` gate passed. The prohibited workspace coverage command was not run. | RESOLVED |
| 707 | Pre-fix `crates/khive-mcp/src/serve.rs:1201-1222` rejected every concrete `--db`/`KHIVE_DB` override when `[[backends]]` existed, including an override canonically equal to the declared `main` path. | Added the canonical equality helper at `crates/khive-mcp/src/serve.rs:1670-1703` and a no-op acceptance arm at `crates/khive-mcp/src/serve.rs:1201-1217`; divergent concrete overrides remain rejected. | All four `concrete_db_override_*` tests passed: two matching-main acceptance cases and two divergent-path rejection cases. The full `khive-mcp` gate also passed. | RESOLVED |

## Conflict register

- Issue 539 was initially reported as resolved, but static review found that its new tests did not force the intended race, could leak test-global state, and could not observe a ninth retry connection. The remediation log records all three corrections; the final tester reran both corrected tests 10/10 and the combined daemon module 80/80. The remediated result controls.
- Issue 544 was initially reported as fully extracted while `round_trip` remained local. The remediation moved it into the shared child module, and the final single-definition scan plus the runtime gate resolved the discrepancy.
- Issue 557's filed acceptance requested kill/respawn on explicit version mismatch, while current source makes every post-write mismatch terminal under #644. Because retry could duplicate an already executed mutation, the source contract and terminal-path test control; the issue is skipped rather than given a contradictory fix.
- Issue 705's original workspace coverage failure was not reproduced because this batch explicitly prohibited workspace cargo commands. Resolution is supported by the static writer-timeout path, the one-thread focused suite, the precisely scoped workflow change, and the full affected-crate gates; this artifact does not claim that workspace coverage itself was run.

## Provenance

- Issue 539: issue 539 investigation; implementation change log; remediation log sections 539-A, 539-B, and 539-C; final test results sections 2 and 3.
- Issue 544: issue 544 investigation; implementation change log; remediation log section 544; final test results sections 1, 2, and 3.
- Issue 557: issue 557 investigation; implementation change log; remediation no-change check; final test results terminal-path check.
- Issue 705: issue 705 investigation; implementation change log; remediation no-change check; final test results CI scoping check.
- Issue 707: issue 707 investigation; implementation change log; remediation no-change check; final test results concrete-override tests.

All final scoped gates passed for the two touched Rust crates: formatting, check, clippy with warnings denied, and tests. No workspace cargo command was run. Nothing was committed, pushed, or used to close an issue during validation.
