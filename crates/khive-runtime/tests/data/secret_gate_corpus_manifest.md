# secret_gate corpus replay manifest

Sanitized aggregate evidence for the `#1040`/`#1056` drop rationale cited in
PR #1062 — generated by the `#[ignore]`d
`secret_gate::corpus_replay::generate_corpus_manifest` test (see that test's
doc comment for the exact command). Contains counts and `sha256` digests of
blocked content only — **never the candidate text itself** — so this file
carries no production data.

Re-run manually and replace this file whenever the detector set changes
enough to warrant a fresh snapshot; this is a point-in-time artifact, not a
CI-enforced check.

## Run

- Date: 2026-07-16
- Corpus: a local read-only copy of the production `khive.db` (`notes.content`
  where `deleted_at IS NULL`, `entities.description` where `deleted_at IS
  NULL`)
- Command: `KHIVE_REPLAY_DB=<copy path> cargo test -p khive-runtime --release
  -- --ignored --nocapture generate_corpus_manifest`

## Results

| Metric | Value |
| --- | --- |
| Total strings scanned | 64047 |
| Total blocked | 63 |
| Block rate | ~0.098% |

### Counts by detector

| Detector | Count |
| --- | --- |
| `hex-credential-token` | 45 |
| `high-entropy-token` | 14 |
| `url-userinfo` | 3 |
| `uuid-near-trigger` | 1 |

### Scope note on the #1040/#1056 claim

The PR body's "#1040 corpus has no run reaching the floor" claim is about a
*different* population than what this replay measures: real file-path
content that is a **false positive near a trigger word** (the class
`is_structured_identifier`'s `!near_trigger` gate would otherwise exempt).
Because that class is, by construction, content the gate does not block, it
does not appear in this replay's `blocked_content_sha256_by_detector` output
— a scan of *currently blocked* content cannot confirm a claim about content
that is *not* blocked. The reproducible evidence for that specific claim is
the checked-in, always-run adversarial regression-test suite in
`secret_gate.rs` (see the block below), not this corpus replay. This replay
instead answers the adjacent, verifiable question the Medium finding also
raised: is the detector's overall false-positive exposure on real production
content small enough to support keeping the `near_trigger` gate unconditional
rather than loosening it? At a 0.098% block rate across 64047 real strings,
yes — and every blocked hash above can be looked up locally against the same
DB to inspect the specific verdict without exposing content in this file.

## Adversarial regression tests guarding the dropped `#1040`/`#1056` fix

The PR body's "12 adversarial regression tests" reference could not be
matched to 12 specific test functions on inspection; the actual named
regression suite for this exact bypass class ("split/pad/disguise a
credential into word-shaped runs to defeat the entropy check") in
`crates/khive-runtime/src/secret_gate.rs` is **10** tests, all under the "──
Structured-identifier exemption drops entirely in trigger context ──"
section:

1. `blocks_separator_secret_access_key_bypass`
2. `blocks_adversarial_lowercase_only_separator_token_near_access_key`
3. `blocks_adversarial_digit_and_word_mixed_token_near_api_key`
4. `blocks_adversarial_token_assignment_separator_delimited_secret`
5. `blocks_extension_suffix_bypass_secret_access_key`
6. `blocks_extension_suffix_bypass_token_assignment`
7. `blocks_unsuffixed_separator_split_credential_bypasses` (2 cases)
8. `blocks_digit_run_suffix_bypass_attempt` (2 cases)
9. `blocks_low_entropy_padding_run_bypass_attempts` (4 cases)
10. `blocks_run_splitting_bypass_attempts` (3 cases)

16 total assertions across these 10 `#[test]` functions. Loosening the
`near_trigger` gate on `is_structured_identifier` (or adding any per-run
entropy-cap variant) to admit the `#1040`/`#1056` real-path false positives
must keep all of these passing, per the module's own soundness argument
(every run-length-N string has the SAME maximum Shannon entropy `log2(N)`
regardless of content — an upper bound, not a claim that entropy is
content-independent at fixed length; two different N-character strings can
and do have different actual entropy, but neither can be distinguished from
the other by an entropy threshold alone when both sit near that shared
ceiling).
