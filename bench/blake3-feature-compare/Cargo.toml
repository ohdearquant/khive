[package]
name = "blake3-feature-compare"
version = "0.0.0"
edition = "2021"
publish = false

[features]
pure = ["blake3/pure"]

[dependencies]
blake3 = { version = "=1.8.5", default-features = false, features = ["std"] }
